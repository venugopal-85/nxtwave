{
    "name": "gadgets-page",
    "version": "1.0.0",
    "description": "Given two files `app.js` and `gadgets.html`, write an API in `app.js` file for the path `/gadgets` that sends the `gadgets.html` file as a response.",
    "main": "app.js",
    "author": "",
    "license": "ISC",
    "dependencies": {
        "express": "4.17.1"
    },
    "devDependencies": {},
    "scripts": {
        "test": "echo \"Error: no test specified\" && exit 1"
    },
    "keywords": []
}